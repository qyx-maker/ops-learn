filebeat.inputs:
- type: container
  paths:
    - /var/lib/docker/containers/*/*.log # 直接读取容器标准输出日志
  processors:
    - add_docker_metadata: ~
    - decode_json_fields:
        fields: ["log"]
        target: ""
        overwrite_keys: true

output.logstash:
  hosts: ["logstash:5044"]

